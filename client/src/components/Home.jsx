import React from 'react';

import SearchBox from './SearchBox';

const Home = (props) => {
    return (
        <div className = 'home-content'>
            <SearchBox/>
        </div>
    )
}

export default Home;